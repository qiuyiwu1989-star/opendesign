interface:
  display_name: "OpenDesign Design Director"
  short_description: "先诊断再生成可安全导入 Studio 的结构化 HTML"
  default_prompt: "Use $opendesign-design-director to diagnose this design task and produce source-grounded Structured HTML for OpenDesign Studio."
