# Install in nginx's `http {}` context, for example under `/etc/nginx/conf.d/`.
# This is intentionally separate from the server-block Studio include.
limit_req_zone $binary_remote_addr zone=opendesign_studio_public:10m rate=30r/m;
